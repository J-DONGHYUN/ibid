# MVP 로드맵 (본인 트랙 — 상품 상세 완성)

> 전체 백로그 → [`BACKLOG.md`](./BACKLOG.md) · 제품 방향 → [`PRODUCT_PLAN.md`](./PRODUCT_PLAN.md)
> 방향: **일반 중고거래 + 검수 에스크로**(전 거래 배송, 직거래 없음). 삭제: 직거래·채팅·사이즈.
> 진행: 이슈 → 브랜치 → PR(`Closes #N`) · 완료 조건: **테스트 포함**([TEST.md](./TEST.md))
> 상태: `[ ]` 대기 / `[~]` 진행 / `[x]` 완료

---

## 완료
- [x] MVP1·2 (인증·상품·구매·주문 상태머신·검수·백오피스), 결제(토스, 종료)
- [x] STAB-1(재고 부풀림) · STAB-6(**RBAC**)
- [x] **상품상태 `condition` enum**(NEW/LIKE_NEW/USED) — 등록폼·상세 연동
- [x] **직거래·채팅·사이즈 삭제**

---

## 현재 — 상품 상세 페이지 실동작화

### Phase 1 — 상호작용
- [x] 상품상태(condition) 표시·등록
- [ ] **공유** (`DTL-1`) — 링크 복사 / Web Share (FE)
- [ ] **찜/좋아요** (`DTL-2`) — like API + **Redis 찜수** + 내 찜여부
- [ ] **조회수** (`DTL-3`) — **Redis 카운터** + 표시

### Phase 2 — 상품 데이터
- [ ] **상품 필드 확장** (`DTL-4`) — `tags` · `createdAt` · **`shippingFee`(플랫폼 관리)**
- [ ] **이미지 업로드(다중)** (`DTL-5`) — S3/파일 + 갤러리

### Phase 3 — 탐색/판매자
- [ ] **판매자 상점/통계** (`DTL-6`) — 판매중 수·거래 횟수 + 상점 페이지
- [ ] **유사 상품 추천** (`DTL-7`) — 카테고리/태그 기반

---

## 이후 (기술 성장 — BACKLOG "이후" 참고)
- 대용량 시딩·인덱스·검색(ES)·캐싱·부하테스트(k6)·관측성·AWS 배포

## 권장 순서
```
DTL-1(공유) → DTL-2(찜) → DTL-3(조회수) → DTL-4(필드) → DTL-5(이미지) → DTL-6·7(상점·추천)
```
- 배송비(`shippingFee`)는 **결제 시 총액에 합산 + 플랫폼 수취**로 설계 (판매자 정산엔 미포함) → 결제/정산과 정합 확인.
