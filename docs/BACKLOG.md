# 개발 백로그 — 현재 작업 (상품 상세 완성)

> **방향**: 일반 중고거래 + **검수 에스크로 유지** ([`PRODUCT_PLAN.md`](./PRODUCT_PLAN.md)) · 전 거래 배송 기반(직거래 없음).
> **삭제 확정**: 직거래 · 채팅 · 사이즈. **배송비 = 플랫폼이 수취·관리**(판매자 정산 미포함).
> **목표**: 기술 성장(성능·인프라) — 찜/조회수(Redis)·이미지(S3) 등이 그 소재.

## ✅ 완료
- STAB-1(재고 부풀림) · STAB-6(RBAC) · 재고 동시성 락 · 결제(토스, **종료**)
- **상품상태 `condition` enum**(NEW/LIKE_NEW/USED) — 등록폼·상세 연동
- **직거래 · 채팅 · 사이즈 삭제** (상세/등록 화면)

## 🎯 현재 작업

### 상품 상세 페이지 실동작화
지금 화면은 UI만 있고 데이터가 하드코딩 → 실제 백엔드 연동으로 전환.

**상태 범례**: ✅ 완료(머지) · 🔵 PR 리뷰 중 · 🟡 로컬 완료(PR 미생성) · ⬜ 미착수

| ID | 작업 | 담당 | 상태 | FE/BE | 기술포인트 |
| --- | --- | --- | --- | --- | --- |
| DTL-1 | **공유** 버튼 (링크 복사 / Web Share) | 동현 | ✅ #32 | FE | - |
| DTL-5 | **상품 이미지 업로드(다중)** + 갤러리 (+수정/삭제) | 동현 | ✅ #32 | BE+FE | 파일·S3 |
| DTL-2 | **찜/좋아요** — `POST/DELETE/GET /api/products/{id}/like` + 관심목록 | 동현 | 🔵 #35 | BE+FE | 유니크 제약·인덱스·동시성 |
| DTL-3 | **조회수** — 조회 시 증가 + 표시 (팀원 PR에서 `PROD-7`로 명명) | 진호 | 🔵 #34 | BE+FE | **Redis 카운터**(Lua 원자성·write-back) |
| DTL-4 | **상품 필드 확장** — `tags` · `createdAt` · `shippingFee` | 동현 | 🟡 `feat/product-fields` | BE+FE | 도메인 모델링 |
| DTL-7 | **유사 상품 추천** — 같은 카테고리/태그 | 진호 | ⬜ | BE+FE | 추천 (DTL-4 `Tag` 활용) |
| DTL-8 | (선택) **신고** 접수 | - | ⬜ | BE+FE | - |
| ~~DTL-6~~ | ~~판매자 상점/통계~~ | - | ❌ **제외** ([#36](https://github.com/J-DONGHYUN/ibid/issues/36)) | - | → 「범위 밖」 |

**권장 순서**: ~~DTL-1~~ → ~~DTL-5~~ → ~~DTL-2~~ → **DTL-4 PR** → DTL-3(팀원) → DTL-7

### 🔧 후속 정리 (리뷰에서 나온 미해결 항목)

머지 전/후에 정리할 것들. 코드리뷰에서 확인된 실제 결함이라 새 기능보다 우선한다.

| ID | 작업 | 우선 | 근거 |
| --- | --- | --- | --- |
| **DTL-4+** | **`shippingFee`를 결제 총액에 합산** | **P0** | 상세에 배송비를 표시하고 문서에도 "결제 시 합산"이라 적었지만, `PurchaseModal`(`price*quantity`)·`Order.create`(`unitPrice*quantity`) 어디에도 더해지지 않아 **실제로 청구되지 않음** |
| DTL-4+ | `tags` 개수·길이 검증 추가 | P1 | `@Size` 없음 + `Tag.name`은 varchar(255) → 255자 초과 시 400이 아닌 500 |
| DTL-4+ | PATCH에서 `tags` 생략 시 전체 삭제되는 동작 정리 | P1 | `resolveTags(null)` → 빈 리스트 → 기존 태그 전멸. 의도인지 명시하거나 부분 수정 지원 |
| DTL-4+ | `createdAt` 상대시간 타임존 보정 | P1 | 존 정보 없는 `LocalDateTime`을 브라우저 로컬로 파싱 → 서버 UTC·뷰어 KST면 방금 등록한 상품이 "9시간 전" |
| DTL-4+ | 고아 `Tag` 정리 | P2 | 아무 상품도 참조하지 않는 태그가 누적됨 |
| **DTL-2+** | **비로그인 사용자에게 찜수 노출** | **P1** | #34가 상품 상세를 `@PublicApi`로 여는데 `GET {id}/like`는 로그인 필수 → 비로그인은 401로 **찜수가 0으로 보임**. 찜수(공개)와 내 찜여부(로그인) 분리 또는 `@PublicApi` 적용 필요 |
| DTL-2+ | 관심목록 페이징 | P2 | `findByUserIdOrderByIdDesc`에 제한이 없어 찜 전체를 한 번에 로드. 홈 목록은 커서 페이지네이션이라 일관성도 어긋남 |
| DTL-2+ | 상품 삭제 시 고아 찜 정리 | P2 | 현재는 관심목록 조회에서 필터링만 함 |
| MERGE-1 | #34·#35 상세 **메타행 병합** | P1 | `👁 조회수` 옆 `❤ 찜수`. 백엔드는 분리 엔드포인트라 충돌 없고 프론트만 겹침 |
| MERGE-2 | 백로그 **ID 체계 통일** | P2 | 같은 조회수 작업을 이 문서는 `DTL-3`, #34는 `PROD-7`/`CACHE-2`로 부름 → 한쪽으로 정리 |
| CONV-1 | PR ↔ 이슈 연결 | P2 | #35에 `Closes #N`이 없다(이슈 미생성). 팀 컨벤션은 이슈→PR |

### 알림 (거래 상태 기반)

이벤트 기반(Spring `ApplicationEvent` + `@TransactionalEventListener(AFTER_COMMIT)`, 검수 이벤트와 동일 패턴). 메시지 큐 미사용(이메일/푸시 붙으면 Kafka로 승격 → "이후").

| ID | 작업 | FE/BE | 기술포인트 |
| --- | --- | --- | --- |
| NOTI-1 | `OrderPaid` 이벤트 발행 — 결제확정 시 `(orderId, sellerId)` | BE | 이벤트 발행 |
| NOTI-2 | 알림 도메인(`Notification`) + 구독자 → **판매자 "결제완료, 발송하세요" 알림** 생성 | BE | 이벤트 구독·도메인 |
| NOTI-3 | 알림 조회/읽음 API — `GET /api/notifications` · `PATCH /{id}/read` · 미읽음 수 | BE | 조회·상태 |
| NOTI-4 | 알림 프론트 — `/notifications` 목록 + 헤더 미읽음 뱃지 | FE | UI |

> 접점(합의): 알림 조회 API 스펙 + `OrderPaid` 이벤트 계약. Defer: 실시간(SSE/푸시)·이메일·알림 설정·다른 알림 종류.

### 분담

**장동현**

| ID | 기능 | FE/BE |
| --- | --- | --- |
| DTL-1 | 공유 | FE |
| DTL-2 | 찜/좋아요 | BE+FE |
| DTL-4 | 상품 필드확장 (tags·createdAt·shippingFee) | BE+FE |
| DTL-5 | 이미지 S3 | BE+FE |
| NOTI-1 | `OrderPaid` 이벤트 발행 | BE |

**김진호**

| ID | 기능 | FE/BE |
| --- | --- | --- |
| DTL-3 | 조회수 | BE+FE |
| DTL-7 | 유사 상품 추천 | BE+FE |
| NOTI-2~4 | 알림 도메인·API·프론트 | BE+FE |

> DTL-6(판매자 상점/통계)은 담당 배정돼 있었으나 **제외 결정** — [#36](https://github.com/J-DONGHYUN/ibid/issues/36)에서 합의 진행 중.

**⚠️ 주의 (찜·조회수 충돌)**
- 같은 파일(`ProductDetailResponse` · 상세 메타행 · `ProductController` · `docs/*`)을 함께 건드림 → **응답 필드·메타행 구조 먼저 합의**
- 실제 진행 결과: **백엔드는 충돌 없음**(찜=별도 컨트롤러·별도 엔드포인트, 조회수=`ProductController`·`ProductDetailResponse`). 겹치는 곳은 **상세 메타행(FE)** 과 **문서 4종**
- 찜은 Redis를 쓰지 않기로 확정(`COUNT`+인덱스). 조회수만 Redis 사용 → 키 컨벤션 충돌 없음
- ⚠️ **#34가 인증 정책을 바꾼다**: `@PublicApi`로 상품 목록·상세를 비로그인에 개방 → 상세에서 호출하는 **찜 상태 조회가 401**이 된다(위 `DTL-2+` P1)

---

## ⏭ 이후 (기술 성장 로드맵)
> 상세 완성 후 착수. 목표(성능·인프라)의 본체.
- 대용량 더미 시딩 · N+1/인덱스 튜닝 · **검색(ES)** · 캐싱 · **부하테스트(k6)+관측성** · **AWS 배포/CI**

## 🚫 범위 밖
- 직거래 · 채팅 · 사이즈 · 결제 정합성 심화 · 커뮤니티(게시판/쿠폰) · 배송사 연동 · 경매
- **DTL-6 판매자 상점/통계** (제외 결정 — [#36](https://github.com/J-DONGHYUN/ibid/issues/36))
  - **마이페이지와 중복**: `/mypage`가 이미 구매/판매 내역 · 판매 중인 상품 · 관심 상품을 제공한다
  - **검수 에스크로가 판매자 신뢰를 대체**: 전 거래를 플랫폼이 검수하므로 구매자가 판매자 평점·거래횟수로 신뢰를 판단할 이유가 구조적으로 적다
  - 상세의 하드코딩 판매자 통계·"상점 보기" 버튼은 #35에서 제거 완료
  - 되살린다면 "판매자의 판매중 상품 목록" 최소 버전만 (통계·평점 제외)

*작성 2026-07 · 일반 중고 + 검수 에스크로, 상품 상세 완성 중심.*
